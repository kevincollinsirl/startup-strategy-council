# OpenAI API Key (optional - only needed if using OpenAI instead of Claude CLI)
OPENAI_API_KEY=

# Optional: Enable authentication (set a secret key to enable)
# AUTH_SECRET=

# Optional: Rate limiting (requests per minute)
# RATE_LIMIT_RPM=60
